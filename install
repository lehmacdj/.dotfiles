#!/bin/bash
# Install script to set up a general unix / linux capable computer as a
# development machine
# Sources all of the files in install.d in addition to some other 
# configurations.

for i in $HOME/.dotfiles/install.d/*.sh; do
    source "$i"
done
